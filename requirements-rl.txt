# Optional — only needed to TRAIN a learned walking/turning policy (bravebot_sim/rl/).
# Train on a GPU box; the base sim (requirements.txt) does not need these.
gymnasium>=1.0
stable-baselines3>=2.0
torch>=2.2
onnx>=1.16
onnxruntime>=1.17
